{
  "name": "vernac",
  "version": "1.0.0",
  "description": "A clean, fast browser extension for translating text on any page.   **Firefox-first. Chrome supported. No account required.**",
  "main": "background.js",
  "scripts": {
    "lint": "eslint background.js content.js popup/*.js shared/*.js",
    "check:messages": "powershell -ExecutionPolicy Bypass -File scripts/check-message-constants.ps1",
    "check": "npm run lint && npm run check:messages",
    "test": "npm run check"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0-only",
  "type": "commonjs",
  "devDependencies": {
    "eslint": "^10.2.1"
  }
}
